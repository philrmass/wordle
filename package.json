{
  "private": true,
  "name": "wordle",
  "version": "0.1.2",
  "license": "MIT",
  "homepage": "https://philrmass.github.io/wordle-stats",
  "scripts": {
    "build": "preact build",
    "deploy": "gh-pages -d build",
    "dev": "preact watch",
    "lint": "eslint src",
    "predeploy": "yarn build",
    "serve": "sirv build --port 8080 --cors --single"
  },
  "eslintConfig": {
    "extends": "preact"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^7.17.0",
    "eslint-config-preact": "^1.1.3",
    "gh-pages": "^4.0.0",
    "preact-cli": "^3.0.0",
    "sirv-cli": "^1.0.3"
  },
  "dependencies": {
    "classnames": "^2.3.1",
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2"
  }
}
